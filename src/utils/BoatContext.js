import { createContext } from "react";

// Contains context for each boat parts scene position (camera) and mesh material (used to highlight selected parts)
export const BoatContext = createContext({});